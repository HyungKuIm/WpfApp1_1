<UserControl x:Class="WpfApp1.Views.CommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Orientation="Vertical">
            <Button Width="75" Height="35" Command="{Binding Test}" CommandParameter="Korea">버튼1</Button>
            <Button Width="75" Height="35" Command="{Binding Test}" CommandParameter="Japan">버튼2</Button>
            <Button Width="75" Height="35" Command="{Binding Test}" CommandParameter="China">버튼3</Button>
            <Image Source="{Binding ImageSource}" Width="400" Stretch="Uniform"/>


        </StackPanel>
          
    </Grid>
</UserControl>
